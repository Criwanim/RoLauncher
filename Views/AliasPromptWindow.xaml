<Window x:Class="RoLauncher.Views.AliasPromptWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Alias da conta"
        Width="460"
        Height="248"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        WindowStyle="SingleBorderWindow">
    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Informe o alias da conta"
                   FontSize="18"
                   FontWeight="SemiBold"/>

        <TextBlock Grid.Row="1"
                   Margin="0,10,0,0"
                   Text="Exemplo: Supporte, Farm, Main, Buff"
                   Opacity="0.75"/>

        <TextBox x:Name="AliasTextBox"
                 Grid.Row="2"
                 Margin="0,16,0,0"
                 Height="38"
                 MinWidth="380"
                 Padding="10,6"
                 FontSize="14"
                 HorizontalAlignment="Stretch"
                 VerticalAlignment="Center"
                 VerticalContentAlignment="Center"
                 MaxLength="40"
                 TextWrapping="NoWrap"
                 AcceptsReturn="False"/>

        <StackPanel Grid.Row="4"
                    Margin="0,10,0,10"
                    Orientation="Horizontal"
                    HorizontalAlignment="Right">
            <Button Width="124"
                    Height="38"
                    Margin="0,0,12,0"
                    Content="Cancelar"
                    Click="Cancel_Click"/>
            <Button Width="124"
                    Height="38"
                    Margin="0,0,12,0"
                    Content="Confirmar"
                    IsDefault="True"
                    Click="Confirm_Click"/>
        </StackPanel>
    </Grid>
</Window>
